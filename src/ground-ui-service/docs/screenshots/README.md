# Screenshots for documentation

Generated by `npm run screenshots` (Playwright). Screenshots are written to **docs/ui** at the repo root.

```bash
npm run screenshots
```

Base URL: `BASE_URL=http://localhost:5180 npm run screenshots`. Full-page; scrollable sections scrolled before capture; Satellite details (09) waits 90s for healthcheck events.
